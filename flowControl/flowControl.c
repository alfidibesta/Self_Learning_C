#include <stdio.h>
#include <ctype.h>

int main(){
    // int number, number1;

    // printf("Enter an integer : ");
    // scanf("%d", &number);
    // scanf("%d %d", &number, &number1);

    /* if else */
    // if (number%2 == 0){
    //     printf("%d is an even integer.", number);
    // } else{
    //     printf("%d is an odd integer.", number);
    // }

    /* loop */
    // for
    // int i, sum = 0;
    // for (i = 0; i < number; ++i)
    // {
    //     // printf("%d", i);
    //     sum += i;

    //     //check logika
    //     // printf("i %d\n", i);
    //     // printf("sum %d\n", sum);
    // }
    // printf("sum %d", sum);

    /* do while loop */
    // double num, sum = 0;
    // // the body of the loop is execute at least once
    // do{
    //     printf("Enter a number: ");
    //     scanf("%lf", &num);
    //     sum += num;
    // }
    // while(num != 0.0);
    // printf("Sum = %.2lf", sum);
    
    // return 0;

    /* break n continue */
    // int i;
    // double num, sum = 0.0;

    // for(i= 1; i<=10;++i){
    //     printf("Enter a n%d", i);
    //     scanf("%lf", &num);

    //     if(num < 0.0){
    //         continue;
    //     }
    //     sum += num;
    // }
    // printf("SUM %.2lf", sum);

    return 0;
}